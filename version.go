package main

/*
	0.1 first release(contains all basic functions)
	0.2 add timeout
	0.3 add group kill
	0.4 use 3rd lib(go-flags,goyaml). add .travel.yml for spec setting + bug fix
	0.5 exit_status not eq 0 when some failed
*/
const VERSION = "0.5"
